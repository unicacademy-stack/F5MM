import { PageShell } from '@/components/page-shell'; import { Card } from '@/components/ui';
export default function Page(){return <PageShell title="Admin / Teacher Panel"><div className="grid md:grid-cols-3 gap-4">{['Upload videos','Upload PDFs','Create quizzes','Monitor progress','Send announcements','Track completion'].map(f=><Card key={f} title={f}>Teacher action module</Card>)}</div><div className="grid md:grid-cols-2 gap-4 mt-4">{['Most replayed videos','Lowest scoring quizzes','Drop-off lessons','Referral conversion rate'].map(a=><Card key={a} title={a}>Analytics insight</Card>)}</div></PageShell>}
